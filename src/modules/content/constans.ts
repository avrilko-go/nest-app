export enum PostType {
    HTML = 'html',
    MD = 'markdown',
}

export enum PostOrderType {
    created = 'createdAt',
    updated = 'updatedAt',

    publishAt = 'publishedAt',

    customer = 'custom',
}
