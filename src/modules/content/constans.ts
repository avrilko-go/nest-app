export enum PostType {
    HTML = 'html',
    MD = 'markdown',
}

export enum PostOrderType {
    CREATED = 'createdAt',
    UPDATED = 'updatedAt',

    PUBLISH_AT = 'publishedAt',

    CUSTOMER = 'custom',

    COMMENT = 'commentCount',
}
